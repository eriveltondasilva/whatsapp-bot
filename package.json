{
  "name": "whatsapp-bot",
  "description": "",
  "keywords": [],
  "version": "1.0.0",
  "main": "dist/index.js",
  "type": "module",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "author": {
    "name": "Erivelton Silva",
    "email": "eriveltondasilva13@gmail.com",
    "url": "https://github.com/eriveltondasilva"
  },
  "scripts": {
    "build": "npm run clean && tsc -b",
    "start": "[ -d dist ] && node --env-file=.env dist/index.js || npm run error",
    "dev": "node --import=tsx --env-file=.env src/index.ts",
    "format": "npx @biomejs/biome format --write ./src",
    "format:check": "npx @biomejs/biome format ./src",
    "lint": "npx @biomejs/biome lint --write ./src",
    "lint:check": "npx @biomejs/biome lint ./src",
    "error": "echo 'Error: dist folder not found. Please run npm run build'",
    "clean": "rm -rf dist",
    "test": ""
  },
  "dependencies": {
    "@wppconnect-team/wppconnect": "^1.35.2",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@tsconfig/node22": "^22.0.0",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "tsyringe": "^4.8.0",
    "typescript": "^5.7.2"
  }
}
